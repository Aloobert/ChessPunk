using UnityEngine;


public class MenuButtonTrigger : MonoBehaviour
{
    public MenuButtonType buttonType;

    // (Optional) You can add visual feedback here (for example, change color on player collision)
}
