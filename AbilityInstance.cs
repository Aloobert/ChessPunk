using UnityEngine;

public class AbilityInstance : MonoBehaviour
{
    // Use the same type as your asset. 
    // If your asset is a GenericAbility (a concrete subclass of AbilityBase), you can declare it as:
    public AbilityBase abilityData;
}
